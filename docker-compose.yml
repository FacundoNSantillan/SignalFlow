services:
  redis:
    image: redis:alpine
    container_name: omnisend-redis
    ports:
      - "${REDIS_PORT:-6379}:6379"
    networks:
      - omnisend-network
    restart: always

networks:
  omnisend-network:
    driver: bridge